//Conexión a la base de datos, esta archivo será requerido en las rutas

const config = {
    user: 'sa',
    password: 'Taller1234',
    server: 'DENNISMERLO', // You can use 'localhost\\instance' to connect to named instance
    database: 'DbSistemaWeb',
}

module.exports = config;